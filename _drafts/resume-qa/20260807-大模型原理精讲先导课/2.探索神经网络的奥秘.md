# 2.探索神经网络的奥秘

内部视角：LLM 的知识是怎么学会的

到底什么是模型？
调整模型内部数学公式的一些参数
一个数学公式 - 约等于一个 LLM

输入
已知
输出

一个数学公式：一条直线分隔蛇和鳄鱼
1.确定一个公式
2.确定公式中的参数是多少

任务越复杂，公式越复杂。人类设计复杂公式是有上限的。万金油公式--神经网络。模拟人脑。
涌现智能
电信号向后传导
神经元之间的连接有粗有细。粗-放大电信号
有正有负
连接线上有一个数字表示粗细
神经网络的数学公式
MNIST 数据库
神经网络的参数设计
确定参数，与公式结构同样重要，比设计公式更难
边缘检测
线形变换
非线形变换
数据有一点零散
数字的信息太过分散
更具备抽象意义的特征
统计学意义的特征
特征提取
一批w参数，对应一种功能
一层一层神经网络计算
使用更少的数字代表原始值
softmax - 归一化，数值相加等于 100%
深度神经网咯 - 深的好处 - 有助于提取更深度，更隐藏的特征信息
深度学习 - 在比较深的神经网络上进行学习
太深会过拟合

神经网络与机器学习
一组不同的参数，神经网络的功能不同
GPT-4 1.8万亿个参数
确定参数：求助于机器学习
机器学习：计算机大规模数学计算，遭到相对更优的一组参数

模拟机器学习过程
开始时：参数随机初始化
计算分类结果的误差
算法工程师：定义误差
累加起来，得到总误差
LLM，视觉识别模型，都需要计算误差
总误差没有绝对的大和小
实验：改变参数的值，使总误差变小一点
误差函数：损失函数 (ax + b -7)^2 ，目的是使总误差最小
穷举
模型训练，模型学习，机器学习 - 目标就是找到 a 和 b 的值使总误差最小
横切一刀，竖切一刀
梯度下降，梯度方向
偏导数
步长，动态变化，开始大，越来越小
发现总误差降低的速度在变慢时，将步长调小
每迈一步，都要计算一下总误差，梯度方向，步长不是固定的
模型收敛了：总误差在某一个小区间内波动
总误差的定义方式 - 损失函数定义
给每一个 w 找到梯度方向
损失函数如何定义
对数
如何基于概率数字计算误差
对到什么程度
算法工程师写的代码知道正确答案
算法工程师写了很多代码，每一个参数自我反省一下，每个参数应该变大还是变小才能使总误差变小，
每个参数找到自己的梯度方向，确认变大还是变小
参数矩阵
`784 * 785
 28 * 785
 10 * 29`
人工智能模型基本做的都是分类任务
真正的大语言模型：10～15万选一
高维空间
现在的 LLM 一般需要上千层

学会的标准 - 能够复述出来
一批段落调一次参数
每一家模型损失函数不一样

特征提取
图片中有哪些内容
文字，是让LLM 读懂文字

一个LLM 中除了神经网络还有别的。
训练的过程中，参数的总数不会发生变化。
先有一个特定的神经网络，再开始训练
找规律，输入和输出之间的规律
公式中的参数，是拟合输入和输出之间的规律

底层是数学计算，但是 LLM 不擅长数学计算
一次特征提取，对应神经网络中的一层，得到的特征数是层中固定的
一次线形 + 一次非线形
非线形运算（不包含任何 w 参数）
线性运算中的 w 参数

训练数据 - 指挥棒
一段文字变成数学向量，需要 LLM
非线性变换是人类智慧的展现
线性变换 - 模拟人脑

只有线性变换，相当于只有一层
只有线性变换不行
大语言模型幻化 - 掌握训练数据中不存在的知识
每一层的线性变换都一样，每一层的非线性变换可以不一样
最后找到的规律对人类来说是不可解释的

问题
回答
