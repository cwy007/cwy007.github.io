# 预训练+微调的训练范式 开源生态和OpenAI的差异详解

站在LLM外面看一下算法工程师如何训练大模型，不去想模型内部原理

讲的同一个模型

从不同角度看

今天将模型内部当成一个黑盒

语言模型训练范式

- GPT 模型的训练阶段

- 详解 Pretrain、SFT、Reward Model、PPO
- Llama

预训练 pretrain: 最大训练数据集，最耗时的步骤

gpt，通义千问，deepseek
LLM：一直在预测输出下一个字
提示词：我给你这些信息，你继续输出
中文常用汉字 3000～5000，全部 8万～10万

训练过程
使用过程

LLM运行机制：给每一个字一个概率，取出前几个概率高的字，然后从这几个概率高的字中取出一个

一个模型，最开始是什么都不会的，怎么学呢？
衡量LLM猜的到底有多不准？
预训练阶段中的一条训练数据是已知的，大模型输出后，可以根据这条已知数据描述LLM的错误程度。
段落联合概率P：让一个模型，能一字不落的生成整个段落的概率。
当前模型的智商水平下，能一字不落的生成整个段落的概率是多少。衡量不准的概率
pretrain 阶段有多少条训练数据，几十亿条训练数据
初始化时，内容是一些随机数学公式
每一条训练数据，都可以要求LLM提高内部的数学公式
LLM内部只有一套数学公式
平均值，通过几百到上千万次迭代
什么时候停止？
标准：整体的和平均的联合概率不能再提高了，就可以结束训练了，没有明确的标准
一张 A100 GPU - 大概25万人民币
得到一个非常熟悉人类语言能力的模型，给一个初始文字，猜下一个文字的能力和人类类似
不同的训练阶段，针对同一个模型，灌输不同的数据
预训练模型说起话来非常像接话茬，不能做任务

pretrain 后的阶段 sft - supervised fine-tuning 有监督的微调训练
有无正确答案
有监督，无监督
有标签，没标签
正向，负向
相似，不相似

自监督，半监督 semi-supervised
问一个问题后，大模型回答后，比较大模型回答和人类提供的答案
自监督：pretrain 的过程就是自监督，大模型回答后，大模型自己评估回答的效果

instruct tuning 指令微调，是 sft 的一种

对话任务
分类任务
SFT 13k条数据
早期，sft阶段的训练数据是人工撰写的。
预训练 -> 后训练
80个博士，给不同领域的问题写答案
step1 搜集数据，训练监督policy，step2收集比较数据，训练reward model

sft后得到一个可以做任务的模型，尤其是对话任务
核心问题：会做任务和能优秀的做任务，还是有很大的差距
问题和答案间放一个特殊的分隔符
训练的时候，模型只能看到问题，看不到答案，让模型预测下一个字，根据模型输出和标准答案来描述模型
模型只能续写任务
问答能力，解析能力 - 本质是续写任务
截止符
一问一答进行训练
sfe后得到的模型
可以续写了，通过续写可以回答问题了

3.reward model 奖励模型，为强化学习做准备
开始使用LLM生成的数据做训练
不是不对，只是不够好，不够全面，不够深度
没对齐
对齐 align
pretrain，sft 是对同一个LLM进行训练
reward model 和 LLM 不是同一个模型
OPEN AI GPT 强化学习，打游戏
强化学习，需要让 LLM 知道结果如何，来评价模型的输出
正向反馈
没有教练在身边，没有办法进行强化学习
alpha go vs 李世石
训练一个奖励模型，作为强化学习的教练
评估是一个很难的事情
评估一个模型的整体能力
评估一个问题回答的如何
在回答到一半时，评估回答的效果
继续训练需要每条逐一评估
准备一系列的提示题prompt，让LLM gei每个prompt 生成多个 response
几万到几十万条 prompt
一个问题回答好几遍，每个问题得到多个答案
要标准工程师对每个问题做怎样的标注
如何标注，打分，评级，排序
肯尼亚2美元标注工
奖励模型需要看懂问题和答案，输出一个分数
到底人类认为什么答案是一个好答案
preference data 偏好数据

PPO - proximal policy optimization - 强化学习的一种
desision making
调整内部数学公式
时间长了，会发现问题：教练员不合格了
用新的 LLM 生成新的 4 个答案，重新训练 reward model

step2 -> step3 -> step2 -> step3
reward model 是作为教练员，用于强化学习

pretrain -> SFT -> Reward Model -> PPO
supervised fine-tuning
proximal policy optimization - reinforcement learning

GPT-3

RLHF - 三部曲 SFT -> Reward Model -> PPO
reinforcement learning from human feekback

deepseek V3 和 GPT-3 在训练阶段类似
deepseek R1
创新便细节，优化

Llama 3
Llama 4 和 GPT 3

不只是一套训练步骤，训练范式
meta
facebook
Llama
SFT 100万条
5000万 ～ 1亿美金 （标注费用）
6道轮回
LLM都是如何训练出来的
20个回答，通过 reward model 保留最好的 3 个
sampling 抽样
问答对 - 一问一答
人类偏好对齐
sft 阶段问题，只准备了优秀问答，LLM 不知道不好的答案
偏好数据, a > b > c = d，一问多个答案

reward -> sft -> dpo
LLM 是运动员

Pairwise Annotated and
Specialized Per-Capability Binary
Preference Data
成对注释和专门化的按能力二进制首选项数据
pretrain - reward model - rejection sampling - sft - dpo

Llama 4 翻车了
meta - 美国喜欢重金挖人的公司
豆包 - 国内喜欢重金挖人的公司

QWen - 在开源模型生态里面，头把交椅（开发者中）
deepseek - toC
不开源的模型

pretrain 预训练
SFT - supervised fine-tuning
reward model - 通过偏好数据训练出来
PPO

GPT-3 vs Llama

标注过程
mid-journey
文生图公司

开源出发点

软件知名度
高级版本
软件太复杂，提供额外服务
通义千问 - 阿里云上的服务与开源模型进行关联，带动阿里云上云服务的售卖

reward model 无法跨 model 复用, 智能在特定训练阶段复用
复述出来

训练过程，是不停的做训练的过程，算法工程师

sft 微调 - 一问一答
reinforement learming 强化学习 - 做一个任务，问一下教练做的如何

原理课
AI 项目组
基础内容
交流
教练模型 - 内部数学公式是相同的，怎么来的，当前版本最好的模型

运动员模型 - 内部数学公式是相同的

a > b > c 使用偏好数据训练

教练员过去也是一个运动员

编程的能力有强有弱
human feedback 一般让用户参与进来

reward model 训练的问题一般会复用
PPO 强化学习的一种算法

sft 输入模型的是问题
损失函数

很多视觉模型没有预训练过程

pretrain, sft 都是段落联合概率
reward model 打分不是人类定义的，模型自己想办法找规律
算法工程师写代码训练模型

指令遵循能力
大模型训练数据中有版权书中的数据，需要购买版权

处理数据的工程
强化学习，不停的重复做某些动作

一节原理课听 3 遍正常
