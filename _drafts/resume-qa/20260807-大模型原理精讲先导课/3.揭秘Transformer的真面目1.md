# 3.揭秘Transformer的真面目1

基座模型
人类给 LLM 划分好几个步骤
不同的步骤对应不同的神经网络，承担不同的角色，具有不同的功能

transformer 架构
  文本变token - tokenization
  token 变向量 - embedding - 有神经网络
  在向量中添加位置信息 positional encoding
  深度理解语义 - encoder & decoder
  生成“下一个字”的权重分布 - liner
  将权重分布转换成概率分布 - softmax

不同阶段，不同的功能

subword - 字词，词元化
750 ～ 1000 token
tiktoken
token切分方式-目标：希望token总数不变
麒麟 - 很少拆开来用
每一轮计算都在输出下一个token

2025年录播

向量、空间、特征
数学空间
向量 - 内部可以存储一些信息
one hot 词表 - 读热编码
10万个汉字，常用3000个
纬度过高，过于稀疏
相似度
LLM 步骤
信息密度
高射炮打蚊子 - 浪费
我要理解面前这段文字
位置编码的价值，
并行处理，一次看到所有的字，每个字在整段话的哪个位置上
语义向量，位置向量
embedding 把一个汉字变成一个向量
encoder 把一段话变成一个向量。有神经网络。比较相似度。
decoder - 把数学空间中人类看不懂的东西，变成人类可以看懂的东西
多模态 -
一堆神经网络
encoder 理解，分析，比较语义
decoder 生成，生成图片，生成翻译句子
文生图
编码器，解码器
650
1300
12288
机器中的看懂和人类的看懂定义不同
设定 - 在语境中理解“问题”的含义。数学向量之间的加法。中心主题词，由这个词出发，去全文中找哪些内容
和这个词语义相似度高-权重高-权重值，聚合信息。每一个token都被当作一个中心主题词，加和到当前token上，
一轮聚合。从别的token上抓一些信息过来。1300tokens -> 1300tokens。第2轮信息聚合，短语和短语之间
做信息聚合，每一个 token 包含的信息更丰富。
Nx - 信息聚合要做多少轮的信息聚合。
一个特殊的token - 告诉 LLM 你可以开始回答问题了
encoder 编码器的计算只计算一次。
decoder 不停的输出，不停的计算。

decoder only 架构：最新的大语言模型的框架。2018 开始。
prompt
LLM 只会做续写任务
bert 模型 encoder only
decoder 理解任何一个词，只能向前看。利用已经出现的词去理解
encoder 理解时，可以看到全文
100万字窗口
理解：用一个数学向量代表100万字

国王 - 男人 + 女人 = 女王

语言学

GPT-3
token 50257
12288 维度
embedding 50257 x 12288
positinal encoding 2048 x 12288
位置编码，最早是一个固定公式（现在已经不用了）。

linear 相关度 - 点乘，点积。这一步没有神经网络，只有一个公式
softmax - 这一步没有神经网络，只有一个公式
单条训练数据上的总误差

decoder - GPT-3 1750亿个参数

batch
epoch
预训练，2 - 3 轮，数据量很大
sft，enforcement learning
自回归

----

训练阶段的描述
训练阶段的细分
transformer 阶段描述

八个锅盖盖不了10口锅
顾头不顾腚
参数越多，拟合能力越强

embedding 是大语言模型训练中的一个步骤。RAG 中的 embedding 和 LLM 训练中的 embedding 不是一回事。
